export function productLandingPath(slug: string): `/products/${string}` {
  return `/products/${slug}`;
}

export function productPurchasePath(slug: string): `/comprar/${string}` {
  return `/comprar/${slug}`;
}
